{
  "name": "password-generator",
  "version": "1.7.5",
  "private": true,
  "description": "本地运行的随机密码、记忆短语与 PIN 生成器",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8765 --bind 127.0.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
