**对照目标**

- 来源截图：`docs/qa/v21-slider-alignment/source-request.png`
- 归一化来源：`docs/qa/v21-slider-alignment/source-normalized.png`
- 实现截图：`docs/qa/v21-slider-alignment/implementation-desktop.png`
- 归一化实现：`docs/qa/v21-slider-alignment/implementation-desktop-normalized.png`
- 并排证据：`docs/qa/v21-slider-alignment/comparison-desktop.png`
- 移动端证据：`docs/qa/v21-slider-alignment/implementation-mobile-390.png`、`docs/qa/v21-slider-alignment/implementation-mobile-320.png`
- 来源像素：1646 × 1152；归一化来源：1640 × 1152。
- 实现区域像素：1640 × 1150；上下补白 1 像素后归一化为 1640 × 1152。
- 桌面 CSS 视口：1648 × 1400，`deviceScaleFactor = 2`；移动端 CSS 视口：390 × 844、320 × 700，`deviceScaleFactor = 1`。
- 状态：Password，复杂度 L2，密码长度 6，生成数量 1。

**完整视图对照**

- 三条滑杆保留现有 V2.1 横向布局、中文标签、快捷刻度、精确输入框与单位，仅修正坐标和图层，不改变用户已确认的信息架构。
- 原截图中滑块圆心相对选中卡片中心偏右 6.44～9 像素；修复后桌面三条滑杆的中心偏差分别为 0.013、0.010、0 像素。
- 轨道的蓝色/橙色填色终点、原生滑块圆心和选中数值卡片现在共享同一条扣除两端 9 像素后的有效行程。

**重点区域对照**

- 字体与排版：未修改字体、字号、字重和中文层级；L1～L8、长度、数量的标签仍保持单行可读。
- 间距与布局：桌面配置区域宽度、标签列、输入框和单位位置均未漂移；320/390 像素下滑杆仍保留横向浏览能力且页面无全局横向溢出。
- 色彩与令牌：选中刻度线从蓝色 2 像素改为与其他刻度一致的 `#aebfb8` 灰色 1 像素；选中数值卡和滑块继续使用原语义色。
- 图片与图标：本区域没有图片或图标资产，未新增替代图形。
- 文案与内容：所有中文标签、辅助说明、精确值和单位保持不变。
- 交互与无障碍：滑杆仍支持鼠标拖动、刻度点击、键盘 Home/End/方向键和精确数字输入；灰色刻度层级为 1，滑杆层级为 2，刻度不会穿过滑块。

**Findings**

- 未发现仍待处理的 P0 / P1 / P2 视觉、响应式或交互问题。
- 未保留 P3 视觉项；原有偏差已经通过几何断言锁定。

**Comparison History**

- 第 1 次（修复前）：用户截图和浏览器测量确认 P2 对齐问题。复杂度、长度、数量的选中刻度中心相对原生滑块圆心分别偏移 6.435、7.369、9 像素；选中刻度为蓝色 2 像素并与滑块处于同一视觉层。
- 修复：为滑杆定义 18 像素滑块尺寸和 9 像素半径；轨道背景宽度改为 `calc(100% - 18px)`；刻度行左右内缩 9 像素；选中刻度改为灰色 1 像素；滑杆和刻度层级分别设为 2 和 1。
- 第 2 次（修复后）：并排图确认滑块、填色终点和选中卡片在同一中心线上。桌面偏差最大 0.013 像素；390 像素最大 0.011 像素；320 像素最大 0.011 像素。选中刻度计算色为 `rgb(174, 191, 184)`，宽度 1 像素。

**Implementation Checklist**

- [x] 复杂度滑块与 L1～L8 刻度精确对齐
- [x] 密码长度滑块与快捷数值精确对齐
- [x] 生成数量滑块与快捷数值精确对齐
- [x] 选中刻度保持灰色 1 像素
- [x] 刻度图层低于滑块图层
- [x] 桌面、390 像素和 320 像素浏览器几何验收
- [x] 键盘、点击、拖动和精确值双向同步保留

final result: passed
