# V2.1 紧凑批量结果区视觉验收

- 源视觉真值：`/var/folders/wj/k5k4njyd16zcxv078rqwhbfc0000gn/T/codex-clipboard-b7d9b587-8b57-49d4-b5de-f447d261e35b.png`
- 实现截图：`/Users/aa/Documents/my/codex/Password-Generator/artifacts/v21-compact-results-413-final.png`
- 并排对照：`/Users/aa/Documents/my/codex/Password-Generator/artifacts/v21-qa-comparison-final.png`
- 页面：`index-2.1.html#password`
- 状态：5 条密码结果；第 2 条已执行原位重新生成；批次级安全分析与生成记录默认折叠；结果默认明文。

## 视口与密度归一化

- 源图：826 × 1960 px，按 Retina 2× 截图推定为 413 × 980 CSS px。
- 实现：413 × 980 CSS px、deviceScaleFactor 1；结果面板元素截图为 389 × 858 px。
- 对照归一化：实现截图等比放大 2× 为 778 × 1716 px，水平居中放入 826 × 1960 px 白底，再与源图并排组成 1652 × 1960 px 对照图。
- 对比仅判断结果面板内容，不把浏览器边缘、页面外边距或源图裁切差异列为问题。

## 全视图对照结论

- 信息架构与源图一致：顶部批量操作、编号结果列表、每条复制/重新生成操作、列表底部统一安全分析、再下方生成记录。
- 新实现把 5 条结果和折叠的批次分析完整放入一个 413 × 980 视口；源图约显示 3 条及展开分析。该密度变化是本次需求的目标，不是视觉漂移。
- 九类生成器共用现有 V2.1 面板、边框、圆角、蓝色主操作、危险红色和等宽结果字体，没有引入新的视觉体系。

## 聚焦区域对照

聚焦检查了结果行、批次分析摘要和 History 摘要三个区域：

- 结果行：序号、密码、组成摘要与操作图标均在稳定网格内；长值单行截断，hover/focus 显示完整值；移动端操作区换至第二行，未发生裁切或横向溢出。
- 批次分析：只出现一次，默认折叠；摘要保留批次数量与最小熵，完整“精确生成器指标 / 观察模式估算 / 攻击场景估算 / 生成模型详情”在展开后显示。
- History：继续默认折叠并保持顶层启用开关，未抢占结果首屏空间。

## 必查视觉表面

- 字体与排版：结果使用项目既有等宽字体，中文标题与说明沿用 V2.1 字体栈；密码、元信息和操作层级清楚，未出现意外换行或过细字重。
- 间距与布局节奏：结果行高度和虚线分隔稳定；窄屏为两行网格，宽屏为单行网格；批次分析与 History 间距一致。
- 颜色与视觉令牌：沿用 `--blue`、`--blue-deep`、`--muted`、`--danger` 与既有边框色；模式风险提示使用现有 warning 语义色。
- 图标与资产：复制、删除、显示/隐藏、重新生成和提示图标均复用 V1.7.5 / 项目既有图标路径，没有用 emoji、文字字符或临时占位符替代。
- 文案与内容：九类生成器结果标题保持中文；共享数学指标与逐条观察模式明确分层；单条重生成提示明确声明未写入剪贴板。

## 对比迭代历史

1. 首次对照发现 P2：窄屏“5 条共用 1 份模型”标签被网格拉成长条，削弱摘要层级。
   - 修复：为 `.batch-assessment-count` 增加 `justify-self: start`，恢复紧凑胶囊宽度。
   - 同步补强：提示图标支持 hover、focus 与触摸点击；点击批次说明不会误展开详情。
2. 第二次对照发现 P2：自动化截图在单条重生成 toast 尚未消失时捕获，遮挡 History 摘要。
   - 修复：视觉截图等待 toast 完全隐藏后再捕获。
3. 最终对照：未发现仍需修复的 P0 / P1 / P2 视觉问题。

## 剩余 P3 / 可接受差异

- 源图每条只显示复制与重新生成；当前版本还保留提示、隐藏和删除图标。这是用户当前功能要求与安全可用性需求，属于预期差异。
- 源图展开完整强度分析；当前批量模式默认折叠，以满足“一屏看到多条密码”的明确目标。

## 已验证交互

- 批量 5 条紧凑渲染与整批单份安全分析。
- 单条原位重新生成，其他行 id/值/顺序不变。
- 单条重生成不写入剪贴板。
- 明文/隐藏切换不改变结果行几何位置或滚动位置。
- 提示气泡 hover、focus、点击开关与批次 details 事件隔离。
- 320、390、430、780、900、1160、1280、1560、2138、2560 px 等关键宽度无横向溢出。
- 九类生成器均能生成并保持各自安全语义；控制台无 page error。

final result: passed
